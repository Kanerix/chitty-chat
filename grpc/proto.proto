syntax = "proto3";

package chitty_chat;

option go_package = "./pb";

service Chat {
    rpc SendMessage(Message) returns (Message);
}

message User {
    string username = 1;
}

message Message {
    string message = 1;
}
